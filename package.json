{
  "name": "proc-that",
  "version": "0.2.1",
  "description": "proc(ess)-that - easy extendable etl tool for nodejs written in typesript",
  "main": "dist/index.js",
  "typigns": "dist/main.d.ts",
  "scripts": {
    "clean": "rimraf build",
    "pretest": "npm install && typings install && npm run clean && tsc",
    "test": "mocha --ui bdd --recursive ./build",
    "develop": "npm run clean && tsc -w",
    "build": "rimraf build dist && tsc --outDir dist -d && trash dist/**/*.spec* && trash dist/Etl.spec* && typings bundle --out dist && trash dist/browser.d.ts"
  },
  "keywords": [
    "etl",
    "node",
    "typescript"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:buehler/proc-that.git"
  },
  "bugs": "https://github.com/buehler/proc-that/issues",
  "author": "Christoph Bühler <christoph.buehler@bluewin.ch>",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "mocha": "^2.4.5",
    "rimraf": "^2.5.2",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0",
    "trash-cli": "^1.2.1",
    "typescript": "^1.8.9",
    "typings": "^0.7.11"
  },
  "dependencies": {
    "es6-promise": "^3.1.2",
    "rxjs": "^5.0.0-beta.4"
  }
}
